drscala {
    simplify-if = warn
    nothing-inferred = warn
    isDefined-get = warn
    case-class-with-array = warn
    find-on-set = warn
    find-on-map = warn
    asInstanceOf = warn
    get-getOrElse = warn
    map-getOrElse = warn
    unsafe-on-empty-iterable = ignore
    println = ignore
    missing-implementation = ignore
    check-no-return-type = warn
}
